# secrets — never commit these
.env
firebase-service-account.json

# local artifacts
captures/
dataset_export/
stage1_dataset/
runs/
runs_stage1/
.cache/
*.jpg
*.png
!README_assets/*

# python
__pycache__/
*.pyc
venv/
.venv/

# model weights (can be large — track separately if needed)
*.pt
